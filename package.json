{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "pretest": "lerna run lint",
    "test": "lerna run test",
    "lint": "lerna run lint"
  },
  "husky": {
    "pre-commit": "lint-staged",
    "pre-push": "npm test"
  },
  "lint-staged": {
    "*.{js,ts,json,scss,css,md,yml}": [
      "prettier --write",
      "git add"
    ]
  },
  "devDependencies": {
    "husky": "^1.3.1",
    "lerna": "^3.10.7",
    "lint-staged": "^8.1.0",
    "prettier": "^1.16.1"
  }
}
